Backup DB and restore in DB.
After delete channeldata in new DB.

--Altering Table Column ChannelValue

ALTER TABLE ChannelData
ALTER COLUMN ChannelValue Varbinary(max) 

ALTER TABLE ChannelData_Archive
ALTER COLUMN ChannelValue Varbinary(max) 

-- Setting Table Index To Zero

DBCC CHECKIDENT(ChannelData, RESEED,0)

-- Query For Converting and Transfering Data From SEW2.2 DB To SEW2.3 DB

INSERT INTO [SEW2.3_DATABASE].DBO.ChannelData(ChannelId, ChannelValue,ChannelDataLogTime,Active)
SELECT ChannelId, EncryptByPassPhrase ('bccb9c79-81c2-41c4-b7b6-49ef2587da93' , CONVERT(VARCHAR(MAX),ChannelValue)) AS ChannelValue,ChannelDataLogTime,Active FROM [SEW2.2_DATABASE].DBO.ChannelData;
	

